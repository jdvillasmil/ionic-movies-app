---
phase: 03-media-browsing
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/shared/components/media-card/media-card.component.ts
  - src/app/shared/components/media-card/media-card.component.spec.ts
  - src/app/home/home.page.ts
  - src/app/home/home.page.html
  - src/app/home/home.page.scss
  - src/app/home/home.page.spec.ts
autonomous: true
requirements:
  - BROW-01
  - BROW-03
  - BROW-04
  - BROW-05

must_haves:
  truths:
    - "Home page renders a 2-column card grid with poster image, title, and year for each item — no login prompt shown to guests"
    - "Skeleton cards (IonSkeletonText, 2-column grid layout) show while data is loading and disappear once items arrive"
    - "Infinite scroll appends the next TMDB page of results when user scrolls to bottom; spinner completes and disappears after each load"
    - "Filter icon in the toolbar opens an IonModal containing genre chips, year range inputs, minimum score IonRange, and sort order selector"
    - "Changing any filter control inside the modal immediately re-fetches and updates the list (no Apply button)"
    - "A visual indicator (badge or icon color change) on the filter button shows when non-default filters are active"
    - "Tapping a card navigates to /tabs/media/:mediaType/:id (Phase 4 stub route)"
  artifacts:
    - path: "src/app/shared/components/media-card/media-card.component.ts"
      provides: "Reusable card showing poster, title, year; emits tap event"
      exports:
        - MediaCardComponent
    - path: "src/app/home/home.page.ts"
      provides: "Home page with grid, infinite scroll, filter modal, sort state"
    - path: "src/app/home/home.page.html"
      provides: "2-column grid, skeleton placeholders, IonModal with filter controls"
  key_links:
    - from: "HomePage"
      to: "TmdbService.getTrending() / discoverMedia()"
      via: "ngOnInit calls loadItems() which checks activeFilters state"
      pattern: "tmdbService\\.getTrending|discoverMedia"
    - from: "IonInfiniteScroll (ionInfinite)"
      to: "loadMore() method"
      via: "event binding"
      pattern: "loadMore"
    - from: "MediaCardComponent (tap)"
      to: "Router.navigate(['/tabs/media', mediaType, id])"
      via: "Output() tap or direct routerLink"
      pattern: "tabs/media"
---

<objective>
Build the MediaCardComponent and the fully-functional Home page: 2-column card grid, skeleton loading, infinite scroll, and the live-filter IonModal with genre chips, year range, score slider, and sort order.

Purpose: This is the primary browsing surface for Phase 3. Guests and logged-in users both land here. It depends on TmdbService from plan 03-01.

Output:
- src/app/shared/components/media-card/media-card.component.ts (reusable card)
- src/app/home/home.page.ts + .html + .scss (full implementation)
- Updated home.page.spec.ts and media-card.component.spec.ts with real test assertions
</objective>

<execution_context>
@C:/Users/renny/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/renny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-media-browsing/03-CONTEXT.md
@.planning/phases/03-media-browsing/03-RESEARCH.md
@.planning/phases/03-media-browsing/03-01-SUMMARY.md

<interfaces>
<!-- Types exported from 03-01 TmdbService — use these directly. -->

From src/app/core/services/tmdb.service.ts (created in plan 03-01):
```typescript
export interface MediaItem {
  id: number;
  mediaType: 'movie' | 'tv';
  title: string;
  year: string;
  posterPath: string | null;
  voteAverage: number;
  genreIds: number[];
}

export interface FilterState {
  genreIds: number[];
  minScore: number;
  yearFrom: string | null;
  yearTo: string | null;
  sortBy: 'popularity.desc' | 'vote_average.desc' | 'primary_release_date.desc';
}

export interface TmdbPageResult {
  results: MediaItem[];
  totalPages: number;
}

export interface Genre {
  id: number;
  name: string;
}

// TmdbService public methods:
getTrending(page?: number): Observable<TmdbPageResult>
searchMulti(query: string, page?: number): Observable<TmdbPageResult>
discoverMedia(filters: FilterState, page?: number): Observable<TmdbPageResult>
getGenres(): Observable<Genre[]>
```

From src/app/core/pipes/tmdb-image.pipe.ts:
- `TmdbImagePipe` — use `w185` size for card thumbnails.
- Import: `import { TmdbImagePipe } from '../core/pipes/tmdb-image.pipe';` (adjust relative path from home/).

From established project patterns:
- Standalone components with imports from `@ionic/angular/standalone`.
- `inject()` function for DI.
- No constructor injection in components (use inject() in class body).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MediaCardComponent (reusable poster card)</name>
  <files>src/app/shared/components/media-card/media-card.component.ts, src/app/shared/components/media-card/media-card.component.spec.ts</files>
  <behavior>
    - Test: Given MediaItem { title: 'Spider-Man', year: '2021', posterPath: '/abc.jpg', mediaType: 'movie', id: 1, voteAverage: 7, genreIds: [] }, the rendered card contains the text 'Spider-Man' and '2021'.
    - Test: When posterPath is null, the card renders without crashing (fallback placeholder or empty src).
    - Test: The component accepts a MediaItem @Input() named 'item'.
  </behavior>
  <action>
    Create src/app/shared/components/media-card/media-card.component.ts:
    - Standalone component.
    - `@Input() item!: MediaItem` — the only input needed.
    - Imports: `IonCard, IonCardContent, IonImg` from `@ionic/angular/standalone`, `TmdbImagePipe` from `../../core/pipes/tmdb-image.pipe` (note: from shared/components/media-card, path is `../../../core/pipes/tmdb-image.pipe`), `NgIf` from `@angular/common`.
    - Template: IonCard wrapping an `<ion-img>` using `item.posterPath | tmdbImage:'w185'` and two text elements for title and year.
    - The card should be a `routerLink` target — do NOT add routerLink here. Navigation is handled by HomePage (passes tap output or wraps in anchor). Use `@Output() cardTap = new EventEmitter<MediaItem>()` with `(click)="cardTap.emit(item)"` on the host element.
    - CSS: card fills its grid column width. Image aspect ratio ~2:3 (poster shape). Title truncated to 1 line (text-overflow: ellipsis). Year in smaller muted text below title.
    - Selector: `app-media-card`.

    Update src/app/shared/components/media-card/media-card.component.spec.ts — replace the placeholder stub with real tests matching the behavior block above. Use `TestBed.configureTestingModule({ imports: [MediaCardComponent] })`. Provide a test MediaItem fixture. Check nativeElement text content for title and year. Check no crash when posterPath is null.
  </action>
  <verify>
    <automated>ng test --watch=false --browsers=ChromeHeadless --include=src/app/shared/components/media-card/media-card.component.spec.ts</automated>
  </verify>
  <done>MediaCardComponent spec passes all 3 tests. Component renders title and year from MediaItem input. posterPath null case handled without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Home page — grid, skeleton, infinite scroll, filter modal</name>
  <files>src/app/home/home.page.ts, src/app/home/home.page.html, src/app/home/home.page.scss, src/app/home/home.page.spec.ts</files>
  <behavior>
    - Test: Component creates without errors when TmdbService is mocked (BROW-01 — no auth guard on route, component bootstraps as guest).
    - Test: On ngOnInit, getTrending(1) is called; items array is populated with returned results.
    - Test: loadMore() increments currentPage, calls getTrending/discoverMedia with page+1, appends new results to existing items array (BROW-05).
    - Test: When activeFilters has non-default values (e.g., genreIds: [28]), loadItems() calls discoverMedia instead of getTrending.
    - Test: hasActiveFilters returns true when genreIds is non-empty or minScore > 0 or yearFrom/yearTo is set.
  </behavior>
  <action>
    Implement src/app/home/home.page.ts:

    Properties:
    - `items: MediaItem[] = []`
    - `isLoading = true`
    - `currentPage = 1`
    - `totalPages = 1`
    - `get allLoaded(): boolean { return this.currentPage >= this.totalPages; }`
    - `filterModalOpen = false`
    - `genres: Genre[] = []`
    - `activeFilters: FilterState = { genreIds: [], minScore: 0, yearFrom: null, yearTo: null, sortBy: 'popularity.desc' }`
    - `get hasActiveFilters(): boolean` — returns true if any filter deviates from defaults
    - `get activeFilterCount(): number` — count of active filter criteria
    - `private tmdbService = inject(TmdbService)`
    - `private router = inject(Router)`

    Methods:
    - `ngOnInit()` — calls `loadGenres()` then `loadItems(true)`.
    - `loadItems(reset = false)` — if reset, sets `currentPage = 1`, `items = []`, `isLoading = true`. Calls `discoverMedia(activeFilters, currentPage)` if hasActiveFilters, else `getTrending(currentPage)`. On result: appends to items (if reset, replaces), sets `totalPages`, sets `isLoading = false`.
    - `loadMore(event: any)` — checks allLoaded (call complete + disable if so), else increments currentPage, fetches, appends items, calls `event.target.complete()` in both success and error paths (critical: always call complete or spinner hangs).
    - `loadGenres()` — calls `tmdbService.getGenres()`, stores in `this.genres`.
    - `onFilterChange()` — calls `loadItems(true)` (resets to page 1 with new filters).
    - `toggleGenre(genreId: number)` — adds/removes from activeFilters.genreIds, calls onFilterChange().
    - `navigateToDetail(item: MediaItem)` — `router.navigate(['/tabs/media', item.mediaType, item.id])`.

    Standalone imports: `IonHeader, IonToolbar, IonTitle, IonContent, IonGrid, IonRow, IonCol, IonButton, IonIcon, IonBadge, IonModal, IonChip, IonLabel, IonRange, IonSelect, IonSelectOption, IonSkeletonText, IonInfiniteScroll, IonInfiniteScrollContent` from `@ionic/angular/standalone`. Add `NgFor, NgIf, AsyncPipe` from `@angular/common`. Add `FormsModule` from `@angular/forms`. Add `MediaCardComponent` from `../shared/components/media-card/media-card.component`. Add `RouterModule` from `@angular/router` (for routerLink if used) or use Router.navigate.

    Implement src/app/home/home.page.html:
    - Toolbar: title "Inicio" + filter button (ion-icon name="options-outline"). Show ion-badge with activeFilterCount if hasActiveFilters (per Claude's discretion: red badge, positioned absolute on button).
    - Skeleton state (when isLoading): 10 skeleton cards in same 2-column grid as real cards — each with IonSkeletonText (height 200px for image, 16px for title, 12px for year, all animated).
    - Real content (when !isLoading): CSS grid with `app-media-card` for each item; (cardTap) handler calls navigateToDetail($event).
    - IonInfiniteScroll at bottom: `(ionInfinite)="loadMore($event)"` `[disabled]="allLoaded"`. Content: `<ion-infinite-scroll-content loadingText="Cargando...">`.
    - IonModal `[isOpen]="filterModalOpen"` `(didDismiss)="filterModalOpen = false"`:
      - Header "Filtros" with Done button that sets `filterModalOpen = false`.
      - Genre section: renders IonChip for each genre. Selected genres use `color="primary"`, unselected use default/outline. Click calls `toggleGenre(genre.id)`.
      - Year range: two number inputs labeled "Desde" and "Hasta" with `[(ngModel)]` bound to activeFilters.yearFrom and activeFilters.yearTo; `(ngModelChange)="onFilterChange()"`.
      - Minimum score: IonRange `min="0"` `max="10"` `step="0.5"` `[(ngModel)]="activeFilters.minScore"` `(ionChange)="onFilterChange()"`.
      - Sort order: Three IonChip or IonSelect options — "Más populares" (popularity.desc), "Mayor puntuación" (vote_average.desc), "Más recientes" (primary_release_date.desc). Clicking one sets activeFilters.sortBy and calls onFilterChange().

    Implement src/app/home/home.page.scss:
    - `.media-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; padding: 12px; }`
    - `.filter-btn { position: relative; }` (for badge absolute positioning)
    - Card images should maintain 2:3 aspect ratio.

    Update src/app/home/home.page.spec.ts: Replace xit stubs with real test implementations matching the behavior block.
  </action>
  <verify>
    <automated>ng test --watch=false --browsers=ChromeHeadless --include=src/app/home/home.page.spec.ts</automated>
  </verify>
  <done>All home.page.spec.ts tests pass. loadMore appends items (BROW-05 green). Component creates without auth (BROW-01 green). discoverMedia called when filters active (BROW-03/BROW-04 path exercised). ng build exits 0.</done>
</task>

</tasks>

<verification>
ng test --watch=false --browsers=ChromeHeadless --include=src/app/home/home.page.spec.ts --include=src/app/shared/components/media-card/media-card.component.spec.ts
</verification>

<success_criteria>
- MediaCardComponent renders poster (w185), title, and year from MediaItem input.
- Home page bootstraps without auth guard; getTrending called on init; items array populated.
- Skeleton cards shown while isLoading; disappear on data arrival.
- Infinite scroll: loadMore() increments page and appends to items array; event.target.complete() always called.
- Filter modal opens with genre chips, year inputs, score range, sort selector.
- Changing any filter triggers immediate list reload (no Apply button).
- hasActiveFilters drives filter badge visibility.
- Card tap navigates to /tabs/media/:mediaType/:id.
- All targeted spec tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-media-browsing/03-02-SUMMARY.md`
</output>
