---
phase: 03-media-browsing
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
autonomous: false
files_modified: []
requirements:
  - BROW-01
  - BROW-02
  - BROW-03
  - BROW-04
  - BROW-05

must_haves:
  truths:
    - "Guest (no login) can open the app, see a populated 2-column card grid of movies and series on the home page"
    - "Searching for a title in the Search tab returns results; a second identical search shows results instantly from cache (no visible TMDB network call in DevTools)"
    - "Home page filter modal opens, genre chips select correctly, list updates immediately without tapping Apply"
    - "Changing sort order in the modal reorders the home list visibly"
    - "Scrolling to the bottom of the home list loads more content (infinite scroll)"
  artifacts:
    - path: "src/app/core/services/tmdb.service.ts"
      provides: "TmdbService — confirmed working end-to-end"
    - path: "src/app/home/home.page.ts"
      provides: "Home page — confirmed guest-accessible, filterable, sortable, infinite scroll"
    - path: "src/app/search/search.page.ts"
      provides: "Search page — confirmed debounced, cache-first"
  key_links:
    - from: "Browser DevTools Network tab"
      to: "themoviedb.org requests"
      via: "Manual inspection"
      pattern: "no duplicate TMDB call on second search"
---

<objective>
Human visual verification of all Phase 3 browsing capabilities: guest access, 2-column card grid, search with cache, filter/sort modal, and infinite scroll.

Purpose: Automated tests verify logic but cannot confirm visual rendering, UX flows (modal opens/closes correctly), or the cache-first behavior visible in DevTools. This checkpoint catches anything unit tests missed before phase sign-off.

Output: Phase 3 approved or issues fed back for gap closure.
</objective>

<execution_context>
@C:/Users/renny/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/renny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-media-browsing/03-02-SUMMARY.md
@.planning/phases/03-media-browsing/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build before visual verification</name>
  <files></files>
  <action>
    Run the full Jasmine/Karma test suite to confirm all Phase 3 unit tests pass before the human visual check. Then run ng build to confirm no compilation errors.

    Commands:
    1. `ng test --watch=false --browsers=ChromeHeadless`
    2. `ng build --configuration=development`

    If any test fails: fix the failing test before proceeding to the checkpoint. Do NOT hand off a failing build to the human checkpoint.
  </action>
  <verify>
    <automated>ng test --watch=false --browsers=ChromeHeadless && ng build --configuration=development 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite passes (all green). ng build exits 0. Ready for human visual check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 3 browsing requirements (BROW-01 through BROW-05)</name>
  <what-built>
    Phase 3 media browsing — all five requirements:
    - BROW-01: Guest home page with 2-column card grid (trending movies + series)
    - BROW-02: Search page with debounced search and Firestore cache-first
    - BROW-03: Home filter modal (genre chips, year range, score slider, live updates)
    - BROW-04: Sort order controls in the filter modal
    - BROW-05: Infinite scroll appending more content on scroll
  </what-built>
  <how-to-verify>
    Run the app locally: `ionic serve` or `ng serve`

    **BROW-01 — Guest home page (no login required):**
    1. Open the app without logging in (clear session if needed: DevTools → Application → Clear Storage).
    2. Navigate to the Home tab (/tabs/home).
    3. Verify: a 2-column grid of movie/series cards loads automatically (poster image, title, year on each card).
    4. Verify: NO login prompt appears.
    5. Verify: skeleton loading cards appear briefly before content arrives.

    **BROW-02 — Search with cache:**
    1. Open the Search tab (/tabs/search).
    2. Open Chrome DevTools → Network tab → filter by "themoviedb.org".
    3. Type "spider" in the search bar — verify results appear (up to 20 cards).
    4. Clear the search bar, then type "spider" again.
    5. Verify: NO new request to themoviedb.org appears in Network tab on the second search (results load instantly from Firestore cache).

    **BROW-03 — Live filter modal:**
    1. On the Home tab, tap the filter icon in the toolbar.
    2. Verify: modal opens with genre chips, year inputs, score slider, and sort options.
    3. Tap any genre chip — verify: the home list updates immediately (no Apply button needed).
    4. Verify: the modal stays open after the list updates.
    5. Tap "Hecho" / "Done" — verify: modal closes.
    6. Verify: a badge or color change on the filter icon indicates active filters.

    **BROW-04 — Sort order:**
    1. Open the filter modal.
    2. Select "Mayor puntuacion" sort option — verify list reorders (highest rated cards first).
    3. Select "Mas recientes" — verify list reorders (most recent release first).

    **BROW-05 — Infinite scroll:**
    1. On Home tab with default filters, scroll to the bottom of the card list.
    2. Verify: a loading spinner appears briefly, then more cards are appended below the existing ones.
    3. Verify: the spinner disappears after new cards load.

    **Navigation:**
    1. Tap any card on Home or Search.
    2. Verify: navigates to the detail placeholder page showing "Detalle de media (proximamente)" with the mediaType and id in the URL.
    3. Verify: back button returns to the previous page.
  </how-to-verify>
  <action>No automated action — human verification step.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; manual verification per how-to-verify above</automated>
  </verify>
  <done>Human types "approved" after verifying all five BROW requirements visually in the running app.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g., "filter modal not opening", "infinite scroll spinner doesn't disappear")</resume-signal>
</task>

</tasks>

<verification>
All five BROW requirements verified by human testing. Full test suite green.
</verification>

<success_criteria>
- Guest sees populated home page without login prompt (BROW-01).
- Second identical search loads from cache — no TMDB network call visible in DevTools (BROW-02).
- Filter modal opens, genre chips work live, list updates without Apply button (BROW-03).
- Sort order changes visibly reorder the list (BROW-04).
- Infinite scroll appends more cards on scroll to bottom (BROW-05).
- Card tap navigates to detail stub route without 404.
- Full ng test suite green.
- Human types "approved".
</success_criteria>

<output>
After completion, create `.planning/phases/03-media-browsing/03-04-SUMMARY.md`
</output>
